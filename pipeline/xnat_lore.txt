Useful stuff:

//Maybe gets the archive path for a certain project
//("this" refers to a BaseElement, I think)
File archive=new File(ArcSpecManager.GetInstance().getArchivePathForProject(this.getProject()));

Site structure:
'->' = 'links to'
Indentations indicate that a file references another file (by parsing or otherwise).

Login.vm
    login_box.vm
    login_row.vm
    site_description.vm
